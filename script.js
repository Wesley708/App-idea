// var decimal = 0;



function enviar(){
    var decimal = document.getElementById('decimal').value;
    calcular(decimal);
}
function calcular(decimal){
    for(decimal; decimal > 0;)
    console.log(decimal);
}